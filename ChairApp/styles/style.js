import { StyleSheet } from 'react-native';

export const useStyling = StyleSheet.create({
    header: {
        fontSize: 40,
        fontFamily: 'Klara',
    },
    
    paragraph: {
        fontSize: 16,
        fontFamily: 'Quicksand',
    },
    
    center: {
        textAlign: 'center',
    }
});